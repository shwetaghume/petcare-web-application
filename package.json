{
  "name": "pet-adoption-system",
  "version": "1.0.0",
  "description": "Pet Adoption System with concurrent frontend and backend",
  "scripts": {
    "start": "cd backend && npm start",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "kill-ports": "npx kill-port 3000 5001",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\""
  },
  "dependencies": {
    "axios": "^1.11.0",
    "concurrently": "^9.2.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
